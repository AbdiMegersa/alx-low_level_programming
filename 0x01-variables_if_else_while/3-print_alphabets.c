#include <stdio.h>

/*
 * main - entry point
 *
 * return: 0 (Succcess)
 */
int main(void)
{
	char low;
	char up;

	for (char low = 'a'; low <= 'z'; low++)
	{
		putchar(low);
	}
	for (char up = 'A'; up <= 'Z'; up++)
	{
		putcahr(up)
	}
	putchar('\n');
	return (0);
}
